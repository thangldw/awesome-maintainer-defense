## Summary

- restore executable quickstarts, issue forms, CODEOWNERS, Dependabot, and least-privilege repository CI;
- add full GitHub write-scope modeling, direct shell-injection detection, and cross-workflow artifact trust paths;
- add JSON/Git baselines, expiring governed suppressions, and pinned pilot evidence;
- ship synchronized 1.1.0 standalone, wheel, sdist, Homebrew, checksum, and ChatGPT/Codex plugin artifacts;
- add tag-gated GitHub Release and PyPI Trusted Publishing automation.

## Verification

- `make test`
- `make validate`
- `make pilot-verify`
- `make package`
- `make distribution-test`
- `make links`
- `make metadata`
- `make pins`
- clean wheel install and plugin ZIP execution smoke tests
- owner-directed pinned dogfood: 0 raw findings, 0 effective findings, no aggregate metric claim

## Release boundary

Merging does not publish. Tag `v1.1.0` triggers the verified release workflow. PyPI publication requires the `maintainer-defense-kit` Trusted Publisher to match repository `thangldw/awesome-maintainer-defense`, workflow `release.yml`, and environment `pypi`.
